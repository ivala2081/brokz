---
// Redirect stub — /admin/wallets → /admin/billing#wallets
// Hash cannot be sent via HTTP redirect, so we use a client-side replace.
---
<!doctype html>
<html>
  <head><meta charset="UTF-8" /><title>Redirecting…</title></head>
  <body>
    <script>location.replace('/admin/billing#wallets');</script>
  </body>
</html>
